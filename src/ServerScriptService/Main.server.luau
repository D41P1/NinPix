local Players = game:GetService("Players")
local WorkspaceFolder = workspace.WorkSpaceFolder
Players.PlayerAdded:Connect(function(player)
    
end)

--[[
    when player joins:
    get data
    for loop script.StarterCharacterScripts {
        clone scripts
        enable the scripts 
        and parent to the WorkSpaceFolder
    } 
]]